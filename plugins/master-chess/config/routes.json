{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "master-chess.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/to-pdf",
      "handler": "master-chess.ToPDF",
      "config": {
        "policies": []
      }
    }
  ]
}
